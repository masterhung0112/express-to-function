{
  "name": "express-to-functions",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "server:build": "tsc -p mono-server",
    "server:watch": "tsc -w -p mono-server",
    "node:start": "node mono-server/dist/index.js",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "express": "^4.17.1",
    "font-awesome": "^4.7.0",
    "rxjs": "^6.6.0"
  },
  "devDependencies": {
    "@types/express": "^4.17.7"
  }
}
